import React from 'react';
import styles from './Head.module.scss';

const Head: React.FC = () => {
    return (
        <div className={styles.container}>
            <h1 >AGENDA CRUD</h1>
        </div>
    );
};

export default Head;
